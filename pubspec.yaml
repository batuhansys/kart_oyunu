name: risk_get_and_gain
description: "RİSK Get and Gain - mobil kart oyunu (Beta 1.0.0, Professional Edition)"
publish_to: 'none'
version: 0.2.0+2

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  go_router: ^14.2.0
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1
  confetti: ^0.7.0
  audioplayers: ^6.1.0
  socket_io_client: ^2.0.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # Ses dosyalarını assets/sounds/ altına ekledikten sonra açın:
  # assets:
  #   - assets/sounds/
